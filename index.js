const divide = function(dividend, divisor) {
    return dividend / divisor;
  }
  
  const square = (num) => num * num;
  
  const add = (a, b) => a + b;
  
  module.exports = {
    divide,
    square,
    add,
  };
  